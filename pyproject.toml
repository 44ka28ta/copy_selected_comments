[project]
name = "copy_selected_comments"
version = "0.3.0"
dependencies = [ "pyperclip" , "pyautogui" , "pathlib" , "opencv-python" , "opencv-contrib-python" ]
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
copy_selected_comments = "copy_selected_comments.gui:main"
